let x; 
     x = 8; 
     x = x +"hi"; 
     // X will convert into string 
     console.log(x, typeof x); 

     //it can't convert "8 hi" to a valid number...
     x = x - 2; 
     console.log(x, typeof x); 
     